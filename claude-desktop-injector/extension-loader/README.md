Not currently used by the CLI (the wrapper is generated inline by
src/patch-asar.js) — kept as a place to grow a standalone wrapper template
if you want to hand-edit it instead of generating it in code.
