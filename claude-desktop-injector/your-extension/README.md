Drop your own extension's electron build here (manifest.json + background.js +
content scripts), built the same way manifest_electron.json describes in
the usage-tracker source. `cli.js patch` / `cli.js install` default to this
folder unless you pass --extension <other-path>.
